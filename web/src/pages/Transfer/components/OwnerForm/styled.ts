import styled from "styled-components";

export const SearchButton = styled.button`
	outline: 0;
	border: 0;
	background-color: #fff;
	cursor: pointer;
	align-self: flex-end;

	img {
		background-color: #fff;
	}
`;
